#!/bin/bash
# Ansible managed, do not edit directly

menu=$($HOME/go/bin/gopass ls --flat | rofi -dmenu -p "gnupass")

if [[ $menu == \|* ]]; then
    eval echo \"$@\" $menu > /dev/null 2>&1 &
else
    eval pass -c $menu \"$@\" > /dev/null 2>&1 &
fi
