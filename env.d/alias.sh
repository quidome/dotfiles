# Generic stuff
[[ $OSTYPE == darwin* ]] || alias ls='ls --color=auto'
alias ll='ls -la'

alias e='emacsclient -t'

# Atom editor
alias a=atom
alias ap='atom -a'

# archlinux
alias y=yaourt

# cssh
alias cssh='i2cssh -p ${ITERM_PROFILE} -b'
